<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.danji.localCurrency.mapper.LocalCurrencyMapper">
    <insert id="create">
        insert into local_currency(localCurrency_id, region_id, benefit_type, maximum, percentage, created_at, updated_at)
            value (#{localCurrencyId}, #{regionId}, #{name}, #{benefitType}, #{maximum}, #{percentage}, now(), now())
    </insert>


</mapper>